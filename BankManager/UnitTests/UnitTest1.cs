using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using OpenQA.Selenium;
using OpenQA.Selenium.IE;
using OpenQA.Selenium.Interactions;

namespace UnitTests
{
    [TestClass]
    public class UnitTest1
    {
        [TestMethod]
        public void TestMethod1()
        {
            //IWebDriver driver = new InternetExplorerDriver(@"C:\WebDriverSrv");
            //IWebDriver driver = new InternetExplorerDriver();
            //string sUrl = "http://www.nice.com";
            ////sUrl = "http://www.nice.com/enterprise-solutions";

            //driver.Navigate().GoToUrl(sUrl);
            //System.Threading.Thread.Sleep(10000);

            //Actions act = new Actions(driver);
            //IWebElement menu = driver.FindElement(By.ClassName("nav-link"));
            //act.MoveToElement(menu).Build().Perform();

            //((IJavaScriptExecutor)driver).ExecuteScript("document.getElementById('dd-our-offering').style.overflow='visible'");
            //driver.FindElement(By.LinkText("Solutions")).Click();
            //driver.FindElement(By.LinkText("Voice of the Customer")).Click();
            //System.Threading.Thread.Sleep(6000);
            //driver.FindElement(By.LinkText("Customer Journey Optimization")).Click();
            //System.Threading.Thread.Sleep(6000);
            //driver.FindElement(By.CssSelector("a[class='thickbox initThickbox-processed']")).Click();
            //ClientController clientCrtl = new ClientController(_clientService,_statusService);
            //Assert.AreNotEqual(clientCrtl, null);
        }
    }
}
